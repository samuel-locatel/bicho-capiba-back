meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "samuellocatel@gmail.com",
    "password": "12345678"
  }
}

vars:pre-request {
  BASE_URL: http://localhost:3333/api
}

settings {
  encodeUrl: true
  timeout: 0
}
